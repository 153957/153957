/* "use strict"; */

$(document).ready(function() {
    var tips = $(".collage");
    tips.tipTip({
        attribute: "data-tip",
        delay: "25",
        defaultPosition: "top",
        edgeOffset: -0
    });
});
